# 🔧 حل مشكلة "فشل في تسجيل الدخول"

## ❌ المشكلة:
عند تسجيل الدخول تظهر رسالة "فشل في تسجيل الدخول"

## 🔍 السبب:
مشكلة في الاتصال بقاعدة البيانات PostgreSQL

---

## ✅ الحل السريع:

### **الطريقة 1: استخدام ملف BAT (الأسهل)**
1. انقر نقراً مزدوجاً على ملف: `حل_مشكلة_قاعدة_البيانات.bat`
2. انتظر حتى ينتهي التشغيل
3. افتح المتصفح: `http://localhost:8080`
4. اضغط "Se connecter" وجرب تسجيل الدخول

### **الطريقة 2: حل مشكلة كلمة مرور PostgreSQL**

#### الخطوة 1: جرب كلمات مرور مختلفة
```bash
# جرب هذه الكلمات بالترتيب:
psql -U postgres -c "SELECT version();"
# إذا طلب كلمة مرور، جرب:
# - postgres
# - admin
# - password
# - (فارغة - اضغط Enter)
```

#### الخطوة 2: إذا نجح الاتصال، أنشئ قاعدة البيانات
```bash
psql -U postgres -c "CREATE DATABASE testdb;"
```

#### الخطوة 3: أنشئ الجداول
```bash
psql -U postgres -d testdb -f database.sql
```

#### الخطوة 4: حدث ملف db.js بكلمة المرور الصحيحة
افتح ملف `backend/db.js` وغيّر:
```javascript
const pool = new Pool({
  user: 'postgres',
  host: 'localhost',
  database: 'testdb',
  password: 'كلمة_المرور_الصحيحة', // غيّر هنا
  port: 5432,
});
```

#### الخطوة 5: شغل الخادم
```bash
cd backend
npm start
```

---

## 🧪 اختبار سريع:

### اختبار PostgreSQL:
```bash
psql -U postgres -c "SELECT version();"
```

### اختبار قاعدة البيانات:
```bash
psql -U postgres -d testdb -c "SELECT * FROM users;"
```

### اختبار الخادم:
```bash
curl http://localhost:3000
```

---

## 📋 كلمات المرور الشائعة لـ PostgreSQL:

| كلمة المرور | الاحتمال |
|-------------|----------|
| postgres | عالي |
| admin | متوسط |
| password | متوسط |
| (فارغة) | منخفض |
| mypassword | منخفض |

---

## 🔧 إذا لم تعمل أي كلمة مرور:

### 1. أعد تعيين كلمة مرور PostgreSQL:
```bash
# في Windows Services
# ابحث عن "postgresql"
# اضغط كليك يمين > Properties > Recovery
# أعد تشغيل الخدمة
```

### 2. أو استخدم كلمة مرور فارغة:
```bash
# في ملف db.js
password: '',
```

### 3. أو أنشئ مستخدم جديد:
```bash
psql -U postgres
CREATE USER myuser WITH PASSWORD 'mypassword';
CREATE DATABASE testdb OWNER myuser;
GRANT ALL PRIVILEGES ON DATABASE testdb TO myuser;
```

---

## 📋 الحسابات التجريبية:

| Email | Mot de passe | Nom |
|-------|-------------|-----|
| admin@example.com | admin123 | Admin |
| ahmed@example.com | password123 | أحمد محمد |
| fatima@example.com | password123 | فاطمة علي |

---

## ⚠️ ملاحظات مهمة:

1. **PostgreSQL يجب أن يعمل**
2. **كلمة مرور PostgreSQL يجب أن تكون صحيحة**
3. **قاعدة البيانات testdb يجب أن تكون موجودة**
4. **الجدول users يجب أن يكون موجوداً**

---

## 🎯 النتيجة المتوقعة:

بعد حل المشكلة:
- ✅ تسجيل الدخول يعمل
- ✅ رسائل بالفرنسية
- ✅ العودة للصفحة الرئيسية
- ✅ إنشاء حسابات جديدة

---

## 📞 للدعم:

راجع الملفات:
- `حل_مشكلة_قاعدة_البيانات.bat` - حل تلقائي
- `debug-login.js` - ملف اختبار
- `db.js` - إعدادات قاعدة البيانات

---

**💡 ابدأ بجرب كلمات مرور PostgreSQL المختلفة!**
