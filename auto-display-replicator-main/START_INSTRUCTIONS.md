# تعليمات تشغيل النظام

## ⚠️ مهم جداً: يجب تشغيل خادمين

### 1. تشغيل قاعدة البيانات (JSON Server)
```bash
npm run db
```
- **المنفذ:** 3001
- **الرابط:** http://localhost:3001
- **الوظيفة:** حفظ التعديلات في قاعدة البيانات

### 2. تشغيل التطبيق (React)
```bash
npm run dev
```
- **المنفذ:** 8080
- **الرابط:** http://localhost:8080
- **الوظيفة:** واجهة المستخدم

## 🔧 طريقة التشغيل الصحيحة

### الطريقة الأولى (مستحسنة):
```bash
# في terminal واحد
npm run dev:full
```

### الطريقة الثانية (يدوياً):
```bash
# Terminal 1 - قاعدة البيانات
npm run db

# Terminal 2 - التطبيق
npm run dev
```

## ✅ التحقق من التشغيل

### 1. قاعدة البيانات:
- افتح: http://localhost:3001
- يجب أن ترى JSON مع المنتجات

### 2. التطبيق:
- افتح: http://localhost:8080
- سجل الدخول: admin/admin123

## 🚨 حل المشاكل

### المشكلة: التعديلات لا تظهر في متصفح آخر
**السبب:** قاعدة البيانات غير مشغلة
**الحل:** 
1. شغل `npm run db`
2. تأكد من أن http://localhost:3001 يعمل
3. أعد تشغيل التطبيق

### المشكلة: خطأ في الاتصال
**السبب:** الخادم غير متاح
**الحل:**
1. تأكد من تشغيل `npm run db`
2. انتظر 5 ثواني
3. أعد تحميل الصفحة

## 📋 خطوات التشغيل الكاملة

1. **فتح Terminal**
2. **الذهاب للمجلد:** `cd auto-display-replicator-main`
3. **تثبيت التبعيات:** `npm install`
4. **تشغيل النظام:** `npm run dev:full`
5. **فتح المتصفح:** http://localhost:8080
6. **تسجيل الدخول:** admin/admin123
7. **بدء التعديل** على المنتجات

## 🔍 التحقق من عمل النظام

### في قاعدة البيانات (http://localhost:3001):
```json
{
  "products": [...],
  "sectionContent": [...],
  "searchOptions": [...]
}
```

### في التطبيق (http://localhost:8080):
- تسجيل الدخول كـ admin
- تعديل منتج
- فتح متصفح آخر
- رؤية التعديلات

## ⚡ نصائح مهمة

1. **لا تغلق Terminal** أثناء العمل
2. **تأكد من تشغيل الخادمين** دائماً
3. **استخدم `npm run dev:full`** للسهولة
4. **تحقق من http://localhost:3001** عند وجود مشاكل
