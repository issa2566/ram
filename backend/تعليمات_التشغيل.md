# 📚 تعليمات تشغيل المشروع بالعربية

## ⚡ خطوات التشغيل

### الخطوة 1: تثبيت المكتبات المطلوبة
افتح Terminal (موجه الأوامر) في مجلد `backend` ثم نفذ:

```bash
npm install
```

هذا الأمر سيقوم بتثبيت جميع المكتبات المطلوبة:
- express (خادم الويب)
- pg (للاتصال بـ PostgreSQL)
- body-parser (لمعالجة البيانات)
- cors (للسماح بالطلبات من مصادر مختلفة)

### الخطوة 2: إعداد قاعدة البيانات PostgreSQL

#### أ. افتح PostgreSQL:
```bash
psql -U postgres
```

إذا طلب منك كلمة المرور، أدخل كلمة المرور الخاصة بـ PostgreSQL (الافتراضية: `mypassword`)

#### ب. أنشئ قاعدة البيانات:
```sql
CREATE DATABASE testdb;
```

#### ج. اخرج من PostgreSQL:
```sql
\q
```

#### د. قم بتنفيذ ملف SQL لإنشاء الجداول:
```bash
psql -U postgres -d testdb -f database.sql
```

هذا الأمر سيقوم بـ:
- إنشاء جدول `users` (المستخدمين)
- إضافة 4 مستخدمين تجريبيين

### الخطوة 3: تشغيل الخادم
```bash
npm start
```

إذا كنت في وضع التطوير وتريد إعادة التشغيل التلقائي:
```bash
npm run dev
```

### الخطوة 4: التأكد من عمل الخادم
افتح المتصفح وادخل على:
```
http://localhost:3000
```

يجب أن ترى رسالة ترحيب مع قائمة نقاط النهاية (Endpoints)

---

## 🧪 اختبار العمليات

### 1. عرض جميع المستخدمين
افتح المتصفح على:
```
http://localhost:3000/users
```

أو باستخدام cURL:
```bash
curl http://localhost:3000/users
```

### 2. إضافة مستخدم جديد
```bash
curl -X POST http://localhost:3000/users \
  -H "Content-Type: application/json" \
  -d "{\"name\":\"خالد أحمد\",\"email\":\"khaled@example.com\"}"
```

### 3. عرض مستخدم محدد (مثلاً المستخدم رقم 1)
```
http://localhost:3000/users/1
```

### 4. تعديل مستخدم
```bash
curl -X PUT http://localhost:3000/users/1 \
  -H "Content-Type: application/json" \
  -d "{\"name\":\"أحمد محمد الجديد\",\"email\":\"ahmed.new@example.com\"}"
```

### 5. حذف مستخدم
```bash
curl -X DELETE http://localhost:3000/users/1
```

---

## 📋 الأوامر المهمة

| الأمر | الوصف |
|-------|-------|
| `npm install` | تثبيت جميع المكتبات المطلوبة |
| `npm start` | تشغيل الخادم |
| `npm run dev` | تشغيل الخادم في وضع التطوير |

---

## 🔧 إعدادات قاعدة البيانات الافتراضية

إذا كانت إعدادات PostgreSQL لديك مختلفة، قم بتعديل ملف `db.js`:

```javascript
const pool = new Pool({
  user: 'postgres',           // اسم المستخدم
  host: 'localhost',          // عنوان الخادم
  database: 'testdb',         // اسم قاعدة البيانات
  password: 'mypassword',     // كلمة المرور
  port: 5432,                 // المنفذ
});
```

---

## ❓ حل المشاكل الشائعة

### مشكلة 1: لا يمكن الاتصال بقاعدة البيانات
**الحل:**
- تأكد من تشغيل PostgreSQL
- تحقق من صحة كلمة المرور في ملف `db.js`
- تأكد من إنشاء قاعدة البيانات `testdb`

### مشكلة 2: error: relation "users" does not exist
**الحل:**
```bash
psql -U postgres -d testdb -f database.sql
```

### مشكلة 3: Port 3000 is already in use
**الحل:**
غيّر المنفذ في ملف `server.js`:
```javascript
const PORT = 3001; // أو أي منفذ آخر
```

### مشكلة 4: npm: command not found
**الحل:**
قم بتثبيت Node.js من الموقع الرسمي: https://nodejs.org/

---

## 📁 ملفات المشروع

```
backend/
├── db.js                    # الاتصال بقاعدة البيانات
├── server.js                # الخادم الرئيسي
├── routes/
│   └── users.js            # مسارات المستخدمين (CRUD)
├── database.sql            # إنشاء الجداول
├── package.json            # معلومات المشروع
├── README.md               # التوثيق الإنجليزي
├── تعليمات_التشغيل.md      # هذا الملف
└── QUICK_START.md          # دليل البدء السريع
```

---

## 🎯 الخلاصة

1. **تثبيت المكتبات:** `npm install`
2. **إنشاء قاعدة البيانات:** `psql -U postgres -c "CREATE DATABASE testdb;"`
3. **إنشاء الجداول:** `psql -U postgres -d testdb -f database.sql`
4. **تشغيل الخادم:** `npm start`
5. **اختبار:** افتح `http://localhost:3000`

✅ **مبروك! المشروع يعمل الآن**

---

## 📞 للدعم

إذا واجهت أي مشاكل:
1. تحقق من سجل الأخطاء في Terminal
2. تأكد من تشغيل PostgreSQL
3. تأكد من تنفيذ جميع الخطوات بالترتيب

---

**صُنع بـ ❤️ باستخدام Node.js و Express.js و PostgreSQL**

