# 🚀 كيفية تشغيل النظام - Rannen Auto Motors

## ⚠️ مهم جداً: يجب تشغيل خادمين

### المشكلة:
التعديلات لا تظهر في المتصفح الآخر لأن قاعدة البيانات غير مشغلة

### الحل:

## 1️⃣ الطريقة الأولى (الأسهل):

### افتح Terminal واكتب:
```bash
cd auto-display-replicator-main
npm run dev:full
```

## 2️⃣ الطريقة الثانية (يدوياً):

### Terminal 1 - قاعدة البيانات:
```bash
cd auto-display-replicator-main
npm run db
```

### Terminal 2 - التطبيق:
```bash
cd auto-display-replicator-main
npm run dev
```

## 3️⃣ الطريقة الثالثة (ملفات التشغيل):

### Windows:
- **انقر مرتين** على `start-servers.bat`

### PowerShell:
- **انقر بزر الماوس الأيمن** على `start-servers.ps1`
- **اختر** "Run with PowerShell"

## ✅ التحقق من العمل:

### 1. قاعدة البيانات:
- افتح: http://69.169.108.182:3000
- يجب أن ترى JSON مع المنتجات

### 2. التطبيق:
- افتح: http://localhost:8080
- سجل الدخول: admin/admin123

## 🔍 اختبار النظام:

1. **سجل الدخول** كـ admin
2. **عدل منتج** (صورة، اسم، سعر)
3. **افتح متصفح آخر**
4. **تأكد من رؤية التعديلات**

## 🚨 حل المشاكل:

### المشكلة: التعديلات لا تظهر في متصفح آخر
**السبب:** قاعدة البيانات غير مشغلة
**الحل:**
1. شغل `npm run db`
2. تأكد من http://69.169.108.182:3000
3. أعد تشغيل التطبيق

### المشكلة: خطأ في الاتصال
**السبب:** الخادم غير متاح
**الحل:**
1. تأكد من تشغيل `npm run db`
2. انتظر 5 ثواني
3. أعد تحميل الصفحة

## 📋 خطوات التشغيل الكاملة:

1. **فتح Terminal**
2. **الذهاب للمجلد:** `cd auto-display-replicator-main`
3. **تثبيت التبعيات:** `npm install`
4. **تشغيل النظام:** `npm run dev:full`
5. **فتح المتصفح:** http://localhost:8080
6. **تسجيل الدخول:** admin/admin123
7. **بدء التعديل** على المنتجات

## ⚡ نصائح مهمة:

- **لا تغلق Terminal** أثناء العمل
- **تأكد من تشغيل الخادمين** دائماً
- **استخدم `npm run dev:full`** للسهولة
- **تحقق من http://69.169.108.182:3000** عند وجود مشاكل

## 🔥 الطريقة السريعة:

```bash
npm run dev:full
```

هذا كل شيء! 🎉
