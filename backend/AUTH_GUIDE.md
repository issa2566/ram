# 🔐 دليل نظام تسجيل الدخول والتسجيل

## ✅ تم إضافة نظام المصادقة الكامل!

---

## 📋 الخطوات المطلوبة:

### 1️⃣ تحديث قاعدة البيانات

قم بتشغيل ملف SQL الجديد لإضافة حقول التسجيل:

```bash
psql -U postgres -d testdb -f database.sql
```

أو إذا كان الجدول موجوداً:

```bash
psql -U postgres -d testdb -f update-database.sql
```

---

### 2️⃣ تثبيت مكتبة bcrypt

```bash
npm install bcrypt
```

---

### 3️⃣ إعادة تشغيل الخادم

```bash
# أوقف الخادم (Ctrl+C) ثم شغله من جديد
npm start
```

---

## 🎯 API Endpoints الجديدة:

### 1. تسجيل مستخدم جديد

**Endpoint:** `POST /auth/register`

**Body:**
```json
{
  "name": "محمد علي",
  "email": "mohamed@example.com",
  "password": "password123",
  "phone": "0612345678",
  "address": "تونس"
}
```

**Response (Success):**
```json
{
  "success": true,
  "message": "تم التسجيل بنجاح",
  "user": {
    "id": 5,
    "name": "محمد علي",
    "email": "mohamed@example.com",
    "phone": "0612345678",
    "address": "تونس",
    "created_at": "2024-01-01T10:00:00.000Z"
  }
}
```

---

### 2. تسجيل الدخول

**Endpoint:** `POST /auth/login`

**Body:**
```json
{
  "email": "ahmed@example.com",
  "password": "password123"
}
```

**Response (Success):**
```json
{
  "success": true,
  "message": "تم تسجيل الدخول بنجاح",
  "user": {
    "id": 2,
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "phone": null,
    "address": null,
    "is_admin": false,
    "created_at": "2024-01-01T10:00:00.000Z",
    "updated_at": "2024-01-01T10:00:00.000Z"
  }
}
```

---

### 3. التحقق من توفر البريد الإلكتروني

**Endpoint:** `GET /auth/check-email/:email`

**Example:** `GET /auth/check-email/test@example.com`

**Response:**
```json
{
  "success": true,
  "available": true
}
```

---

## 🧪 اختبار النظام

### باستخدام cURL:

#### تسجيل مستخدم جديد:
```bash
curl -X POST http://69.169.108.182:3000/auth/register \
  -H "Content-Type: application/json" \
  -d "{\"name\":\"محمد علي\",\"email\":\"mohamed@example.com\",\"password\":\"password123\",\"phone\":\"0612345678\"}"
```

#### تسجيل الدخول:
```bash
curl -X POST http://69.169.108.182:3000/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"ahmed@example.com\",\"password\":\"password123\"}"
```

---

## 🖥️ استخدام Frontend

1. **افتح المتصفح على:**
   ```
   http://localhost:8080
   ```

2. **اضغط على زر "Se connecter" في Header**

3. **سترى صفحة تسجيل الدخول مع خيارين:**
   - تسجيل الدخول
   - إنشاء حساب جديد

4. **لتسجيل الدخول:**
   - استخدم أحد الحسابات التجريبية:
     - البريد: `ahmed@example.com`
     - كلمة المرور: `password123`

5. **لإنشاء حساب جديد:**
   - اضغط على "إنشاء حساب"
   - املأ النموذج
   - اضغط "إنشاء حساب"

---

## 🔒 الأمان

- ✅ **تشفير كلمات المرور:** يتم تشفير كلمات المرور باستخدام bcrypt
- ✅ **التحقق من البيانات:** فحص صحة البريد الإلكتروني وطول كلمة المرور
- ✅ **منع التكرار:** لا يمكن تسجيل نفس البريد الإلكتروني مرتين
- ✅ **رسائل خطأ آمنة:** لا يتم كشف معلومات حساسة في رسائل الخطأ

---

## 📊 الحقول الجديدة في جدول users

| الحقل | النوع | الوصف | مطلوب؟ |
|------|------|-------|-------|
| id | SERIAL | المعرف الفريد | تلقائي |
| name | VARCHAR(100) | اسم المستخدم | نعم |
| email | VARCHAR(100) | البريد الإلكتروني | نعم |
| password | VARCHAR(255) | كلمة المرور المشفرة | نعم |
| phone | VARCHAR(20) | رقم الهاتف | لا |
| address | TEXT | العنوان | لا |
| is_admin | BOOLEAN | هل المستخدم مسؤول؟ | لا (افتراضي: false) |
| created_at | TIMESTAMP | تاريخ الإنشاء | تلقائي |
| updated_at | TIMESTAMP | تاريخ التحديث | تلقائي |

---

## ⚠️ ملاحظات مهمة

1. **تأكد من تشغيل Backend:**
   ```bash
   cd backend
   npm start
   ```

2. **تأكد من تشغيل Frontend:**
   ```bash
   cd auto-display-replicator-main
   npm run dev
   ```

3. **إذا كانت قاعدة البيانات قديمة:**
   - قم بتشغيل `update-database.sql`
   - أو احذف الجدول القديم وأنشئه من جديد

4. **كلمات المرور التجريبية:**
   - جميع المستخدمين التجريبيين يستخدمون كلمة المرور: `password123`

---

## 🎊 المزايا المضافة

- ✅ نموذج تسجيل دخول جميل ومتجاوب
- ✅ نموذج تسجيل حساب جديد
- ✅ التبديل بين التسجيل وتسجيل الدخول
- ✅ رسائل خطأ واضحة بالعربية
- ✅ تأثيرات تحميل (Loading states)
- ✅ حفظ بيانات المستخدم في localStorage
- ✅ إعادة التوجيه للصفحة الرئيسية بعد النجاح

---

## 🐛 حل المشاكل

### المشكلة: خطأ في الاتصال بالخادم
**الحل:**
- تأكد من تشغيل Backend (`npm start` في مجلد backend)
- تأكد من أن الخادم يعمل على http://69.169.108.182:3000

### المشكلة: البريد الإلكتروني مسجل بالفعل
**الحل:**
- استخدم بريد إلكتروني مختلف
- أو احذف المستخدم من قاعدة البيانات:
  ```sql
  DELETE FROM users WHERE email = 'email@example.com';
  ```

### المشكلة: كلمة المرور غير صحيحة
**الحل:**
- تأكد من كتابة كلمة المرور الصحيحة
- للحسابات التجريبية، استخدم: `password123`

---

**🎉 الآن نظام تسجيل الدخول جاهز للاستخدام!**

