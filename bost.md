# 📋 دليل شامل للمشروع - BOST

## 🎯 نظرة عامة على المشروع

هذا المشروع عبارة عن تطبيق ويب كامل (Full-Stack Application) يتكون من:
- **الواجهة الأمامية (Frontend)**: تطبيق React حديث
- **الخادم الخلفي (Backend)**: API RESTful باستخدام Node.js
- **قاعدة البيانات (Database)**: PostgreSQL مع دعم SQLite3 للتطوير

---

## 💻 اللغات البرمجية المستخدمة

### 1. **JavaScript / TypeScript**
   - **الاستخدام**: اللغة الأساسية للمشروع
   - **في Frontend**: TypeScript 5.8.3
   - **في Backend**: JavaScript (Node.js)
   - **الوصف**: 
     - TypeScript في الواجهة الأمامية لضمان جودة الكود واكتشاف الأخطاء مبكراً
     - JavaScript في الخادم الخلفي للسرعة والمرونة

### 2. **HTML5**
   - **الاستخدام**: بنية صفحات الويب
   - **الوصف**: Markup Language لبناء هيكل الصفحات

### 3. **CSS3 / Tailwind CSS**
   - **الاستخدام**: تصميم وتنسيق الواجهة
   - **الإصدار**: Tailwind CSS 3.4.17
   - **الوصف**: إطار عمل CSS Utility-First للتصميم السريع والمرن

### 4. **SQL**
   - **الاستخدام**: استعلامات قاعدة البيانات
   - **الوصف**: لغة الاستعلامات المستخدمة للتفاعل مع PostgreSQL

### 5. **JSON**
   - **الاستخدام**: تنسيق البيانات للتبادل بين Frontend و Backend
   - **الوصف**: تنسيق البيانات القياسي لـ REST API

---

## 🎨 الواجهة الأمامية (Frontend)

### **التقنيات الأساسية:**

#### 1. **React 18.3.1**
   - **الوصف**: مكتبة JavaScript لبناء واجهات المستخدم
   - **الميزات**:
     - مكونات قابلة لإعادة الاستخدام
     - إدارة الحالة (State Management)
     - Virtual DOM للأداء العالي
     - Hooks API

#### 2. **TypeScript 5.8.3**
   - **الوصف**: لغة برمجة مطورة من JavaScript تضيف نظام الأنواع
   - **الميزات**:
     - Type Safety
     - اكتشاف الأخطاء مبكراً
     - IntelliSense محسّن

#### 3. **Vite 5.4.19**
   - **الوصف**: أداة بناء سريعة جداً
   - **الميزات**:
     - Hot Module Replacement (HMR) فوري
     - بناء سريع
     - دعم TypeScript و JSX

#### 4. **Tailwind CSS 3.4.17**
   - **الوصف**: إطار عمل CSS Utility-First
   - **الميزات**:
     - Utility Classes
     - Responsive Design
     - Dark Mode Support

### **مكتبات إضافية:**

- **React Router DOM 6.30.1**: التوجيه (Routing)
- **React Hook Form 7.61.1**: إدارة النماذج
- **Zod 3.25.76**: التحقق من البيانات
- **TanStack Query 5.83.0**: إدارة حالة الخادم
- **shadcn/ui**: مكونات UI مبنية على Radix UI
- **Radix UI**: مكونات UI متاحة (Accessible)
- **Lucide React**: الأيقونات
- **Recharts**: الرسوم البيانية
- **Fuse.js**: البحث الضبابي

### **إعدادات السيرفر (Frontend Server):**

```
- المنفذ (Port): 8080
- العنوان (Host): :: (جميع العناوين)
- الرابط الكامل: http://localhost:8080
- نوع السيرفر: Vite Development Server
- Hot Reload: مفعّل
```

### **مجلدات الواجهة الأمامية:**

```
auto-display-replicator-main/
├── src/
│   ├── components/     # المكونات
│   ├── pages/         # الصفحات
│   ├── api/           # طلبات API
│   ├── hooks/         # Custom Hooks
│   ├── contexts/      # Context API
│   ├── services/      # الخدمات
│   ├── types/         # تعريفات TypeScript
│   └── utils/         # أدوات مساعدة
├── public/            # الملفات الثابتة
└── package.json      # التبعيات
```

---

## ⚙️ الخادم الخلفي (Backend)

### **التقنيات الأساسية:**

#### 1. **Node.js**
   - **الوصف**: بيئة تشغيل JavaScript على الخادم
   - **الميزات**:
     - Non-blocking I/O
     - Event-Driven Architecture
     - NPM Package Manager

#### 2. **Express.js 4.18.2**
   - **الوصف**: إطار عمل Node.js لبناء APIs
   - **الميزات**:
     - RESTful API Development
     - Middleware Support
     - Routing System
     - JSON Parsing

### **مكتبات إضافية:**

- **pg 8.11.3**: عميل PostgreSQL لـ Node.js
- **sqlite3 5.1.7**: عميل SQLite3
- **bcrypt 5.1.1**: تشفير كلمات المرور
- **cors 2.8.5**: Cross-Origin Resource Sharing
- **body-parser 1.20.2**: تحليل بيانات الطلب
- **dotenv 16.3.1**: متغيرات البيئة
- **nodemon 3.0.1**: إعادة تشغيل تلقائية (للتطوير)

### **إعدادات السيرفر (Backend Server):**

```
- المنفذ (Port): 3000
- العنوان (Host): localhost
- الرابط الكامل: http://69.169.108.182:3000
- نوع السيرفر: Express.js HTTP Server
- CORS: مفعّل لجميع المصادر
```

### **مسارات API (Endpoints):**

#### **المصادقة (Authentication):**
- `POST /auth/register` - تسجيل مستخدم جديد
- `POST /auth/login` - تسجيل الدخول
- `GET /auth/check-email/:email` - التحقق من البريد الإلكتروني

#### **المستخدمين (Users):**
- `GET /users` - الحصول على جميع المستخدمين
- `GET /users/:id` - الحصول على مستخدم محدد
- `POST /users` - إنشاء مستخدم جديد
- `PUT /users/:id` - تحديث مستخدم
- `DELETE /users/:id` - حذف مستخدم

#### **البحث (Search):**
- `GET /searchOptions` - الحصول على خيارات البحث
- `POST /searchOptions` - إضافة خيار بحث
- `DELETE /searchOptions/:id` - حذف خيار بحث

### **مجلدات الخادم الخلفي:**

```
backend/
├── routes/
│   ├── auth.js       # مسارات المصادقة
│   └── users.js      # مسارات المستخدمين
├── db.js             # اتصال قاعدة البيانات
├── db-config.js      # إعدادات قاعدة البيانات
├── server.js         # ملف السيرفر الرئيسي
└── package.json      # التبعيات
```

---

## 🗄️ قاعدة البيانات (Database)

### **نوع قاعدة البيانات:**

#### 1. **PostgreSQL** (الإنتاج والتطوير)
   - **الإصدار**: Latest
   - **الوصف**: نظام إدارة قواعد بيانات علائقية متقدم
   - **الميزات**:
     - ACID Compliance
     - Advanced Data Types
     - Full-Text Search
     - JSON Support
     - High Performance

#### 2. **SQLite3** (للتطوير والاختبار)
   - **الإصدار**: 5.1.7
   - **الوصف**: قاعدة بيانات خفيفة الوزن مدمجة
   - **الميزات**:
     - Zero Configuration
     - Serverless
     - File-Based Database

### **إعدادات قاعدة البيانات:**

#### **PostgreSQL:**
```
- العنوان (Host): localhost
- المنفذ (Port): 5432
- اسم المستخدم (User): postgres
- اسم قاعدة البيانات (Database): testdb
- كلمة المرور (Password): (قابلة للتخصيص)
```

#### **SQLite3:**
```
- الملف: database.db (في مجلد backend)
- النوع: File-Based
- لا يحتاج سيرفر منفصل
```

### **أين يتم تخزين البيانات:**

#### **PostgreSQL:**
- **الموقع**: على السيرفر المحلي (localhost)
- **التخزين**: 
  - البيانات مخزنة في ملفات PostgreSQL على القرص الصلب
  - المسار الافتراضي: `C:\Program Files\PostgreSQL\[version]\data` (Windows)
  - أو `/var/lib/postgresql/data` (Linux)
- **النسخ الاحتياطي**: يمكن عمل نسخ احتياطية من قاعدة البيانات

#### **SQLite3:**
- **الموقع**: ملف محلي في مجلد المشروع
- **التخزين**: 
  - ملف واحد: `database.db`
  - موجود في مجلد `backend/`
  - جميع البيانات في ملف واحد

### **هيكل قاعدة البيانات:**

#### **جدول المستخدمين (users):**
```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  email VARCHAR(100) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL,
  phone VARCHAR(20),
  address TEXT,
  is_admin BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### **اتصال قاعدة البيانات:**

#### **في الكود (db.js):**
```javascript
const { Pool } = require('pg');

const pool = new Pool({
  user: 'postgres',
  host: 'localhost',
  database: 'testdb',
  password: '123456',  // قابلة للتغيير
  port: 5432,
});
```

---

## 🌐 السيرفر (Server)

### **1. سيرفر الواجهة الأمامية (Frontend Server)**

#### **Vite Development Server:**
```
- النوع: Development Server
- المنفذ: 8080
- العنوان: http://localhost:8080
- الميزات:
  * Hot Module Replacement (HMR)
  * Fast Refresh
  * Source Maps
  * TypeScript Support
```

#### **كيفية التشغيل:**
```bash
cd auto-display-replicator-main
npm run dev
```

### **2. سيرفر الخادم الخلفي (Backend Server)**

#### **Express.js HTTP Server:**
```
- النوع: REST API Server
- المنفذ: 3000
- العنوان: http://69.169.108.182:3000
- البروتوكول: HTTP
- الميزات:
  * RESTful API
  * JSON Support
  * CORS Enabled
  * Middleware Support
```

#### **كيفية التشغيل:**
```bash
cd backend
npm start
# أو للتطوير مع إعادة التشغيل التلقائي:
npm run dev
```

### **3. سيرفر قاعدة البيانات (Database Server)**

#### **PostgreSQL Server:**
```
- النوع: Database Server
- المنفذ: 5432
- العنوان: localhost
- البروتوكول: PostgreSQL Protocol
- الميزات:
  * Connection Pooling
  * Transaction Support
  * Prepared Statements
```

#### **كيفية التشغيل:**
- يجب تثبيت PostgreSQL على النظام
- السيرفر يعمل كخدمة (Service) في الخلفية
- الاتصال يتم عبر عميل `pg` في Node.js

---

## 🔄 تدفق البيانات (Data Flow)

```
┌─────────────────┐
│   Frontend       │
│   (Port 8080)    │
│   React + Vite   │
└────────┬─────────┘
         │ HTTP Requests
         │ (JSON)
         ▼
┌─────────────────┐
│   Backend API   │
│   (Port 3000)   │
│   Express.js    │
└────────┬─────────┘
         │ SQL Queries
         │ (pg / sqlite3)
         ▼
┌─────────────────┐
│   Database      │
│   PostgreSQL    │
│   (Port 5432)   │
└─────────────────┘
```

---

## 📦 التبعيات الرئيسية (Dependencies)

### **Frontend (package.json):**
```json
{
  "react": "^18.3.1",
  "react-dom": "^18.3.1",
  "typescript": "^5.8.3",
  "vite": "^5.4.19",
  "tailwindcss": "^3.4.17",
  "react-router-dom": "^6.30.1",
  "react-hook-form": "^7.61.1",
  "zod": "^3.25.76",
  "@tanstack/react-query": "^5.83.0"
}
```

### **Backend (package.json):**
```json
{
  "express": "^4.18.2",
  "pg": "^8.11.3",
  "sqlite3": "^5.1.7",
  "bcrypt": "^5.1.1",
  "cors": "^2.8.5",
  "body-parser": "^1.20.2",
  "dotenv": "^16.3.1",
  "nodemon": "^3.0.1"
}
```

---

## 🚀 كيفية تشغيل المشروع

### **1. تشغيل قاعدة البيانات:**
```bash
# تأكد من أن PostgreSQL يعمل
# أو استخدم SQLite3 للتطوير
```

### **2. تشغيل الخادم الخلفي:**
```bash
cd backend
npm install
npm start
# السيرفر سيعمل على: http://69.169.108.182:3000
```

### **3. تشغيل الواجهة الأمامية:**
```bash
cd auto-display-replicator-main
npm install
npm run dev
# السيرفر سيعمل على: http://localhost:8080
```

---

## 📊 ملخص المنافذ (Ports Summary)

| الخدمة | المنفذ | العنوان الكامل |
|--------|--------|----------------|
| Frontend | 8080 | http://localhost:8080 |
| Backend API | 3000 | http://69.169.108.182:3000 |
| PostgreSQL | 5432 | localhost:5432 |
| JSON Server (Dev) | 3001 | http://69.169.108.182:3000 |

---

## 🔐 الأمان (Security)

### **المصادقة:**
- **bcrypt**: لتشفير كلمات المرور
- **JWT**: (يمكن إضافتها) للـ Tokens
- **Session Management**: إدارة الجلسات

### **CORS:**
- مفعّل لجميع المصادر في التطوير
- يجب تقييده في الإنتاج

### **كلمات المرور:**
- مشفرة باستخدام bcrypt
- Salt Rounds: 10 (افتراضي)

---

## 📝 ملاحظات مهمة

1. **قاعدة البيانات**: 
   - PostgreSQL هو الخيار الرئيسي
   - SQLite3 للتطوير السريع

2. **المنافذ**: 
   - تأكد من أن المنافذ 3000 و 8080 و 5432 متاحة

3. **البيئة**: 
   - استخدم ملف `.env` لتخزين الإعدادات الحساسة
   - لا ترفع ملف `.env` إلى Git

4. **التطوير**: 
   - استخدم `nodemon` لإعادة تشغيل السيرفر تلقائياً
   - استخدم Vite HMR لتحديث الواجهة فوراً

---

## 📅 آخر تحديث

تم إنشاء هذا الملف في: **2024**

---

## 📞 الدعم

للمزيد من المعلومات، راجع:
- `README.md` في مجلد backend
- `README.md` في مجلد auto-display-replicator-main
- ملفات التوثيق في كل مجلد

