# 🚀 خادم Node.js مع Express.js و PostgreSQL

مشروع Backend كامل يستخدم Node.js و Express.js و PostgreSQL لإدارة المستخدمين مع جميع العمليات (CRUD).

## 📋 المتطلبات

قبل البدء، تأكد من تثبيت:
- [Node.js](https://nodejs.org/) (الإصدار 14 أو أحدث)
- [PostgreSQL](https://www.postgresql.org/) (الإصدار 12 أو أحدث)

## 🛠️ التثبيت والتشغيل

### الخطوة 1: تثبيت الحزم

```bash
cd backend
npm install
```

### الخطوة 2: إعداد قاعدة البيانات PostgreSQL

1. **تشغيل PostgreSQL:**
   - تأكد من أن خدمة PostgreSQL تعمل على جهازك

2. **إنشاء قاعدة البيانات:**
   ```bash
   # افتح PostgreSQL من الـ Terminal
   psql -U postgres
   
   # أنشئ قاعدة البيانات
   CREATE DATABASE testdb;
   
   # اخرج من PostgreSQL
   \q
   ```

3. **إنشاء جدول المستخدمين:**
   ```bash
   # قم بتنفيذ ملف SQL
   psql -U postgres -d testdb -f database.sql
   ```

   أو بطريقة يدوية:
   ```bash
   psql -U postgres -d testdb
   ```
   
   ثم قم بنسخ محتوى ملف `database.sql` ولصقه في PostgreSQL.

### الخطوة 3: تعديل إعدادات قاعدة البيانات (إن لزم الأمر)

إذا كانت بيانات الاتصال مختلفة، قم بتعديل ملف `db.js`:

```javascript
const pool = new Pool({
  user: 'اسم_المستخدم',
  host: 'localhost',
  database: 'اسم_قاعدة_البيانات',
  password: 'كلمة_المرور',
  port: 5432,
});
```

### الخطوة 4: تشغيل الخادم

```bash
npm start
```

أو للتطوير مع إعادة التشغيل التلقائي:
```bash
npm run dev
```

سيعمل الخادم على: **http://localhost:3000**

## 📡 نقاط النهاية (API Endpoints)

### 1. عرض جميع المستخدمين
```http
GET /users
```

**مثال على الاستجابة:**
```json
{
  "success": true,
  "count": 4,
  "data": [
    {
      "id": 1,
      "name": "أحمد محمد",
      "email": "ahmed@example.com",
      "created_at": "2024-01-01T10:00:00.000Z",
      "updated_at": "2024-01-01T10:00:00.000Z"
    }
  ]
}
```

### 2. عرض مستخدم واحد
```http
GET /users/:id
```

**مثال:**
```bash
GET /users/1
```

### 3. إضافة مستخدم جديد
```http
POST /users
Content-Type: application/json

{
  "name": "اسم المستخدم",
  "email": "email@example.com"
}
```

**مثال باستخدام cURL:**
```bash
curl -X POST http://localhost:3000/users \
  -H "Content-Type: application/json" \
  -d '{"name":"خالد أحمد","email":"khaled@example.com"}'
```

### 4. تعديل مستخدم
```http
PUT /users/:id
Content-Type: application/json

{
  "name": "الاسم الجديد",
  "email": "newemail@example.com"
}
```

**مثال:**
```bash
curl -X PUT http://localhost:3000/users/1 \
  -H "Content-Type: application/json" \
  -d '{"name":"أحمد محمد المحدث","email":"ahmed.new@example.com"}'
```

### 5. حذف مستخدم
```http
DELETE /users/:id
```

**مثال:**
```bash
curl -X DELETE http://localhost:3000/users/1
```

## 🧪 اختبار الـ API

### باستخدام Postman:
1. افتح Postman
2. أنشئ طلبات جديدة باستخدام النقاط أعلاه
3. تأكد من تحديد `Content-Type: application/json` للطلبات POST و PUT

### باستخدام cURL:

```bash
# عرض جميع المستخدمين
curl http://localhost:3000/users

# إضافة مستخدم جديد
curl -X POST http://localhost:3000/users \
  -H "Content-Type: application/json" \
  -d '{"name":"محمد علي","email":"mohamed@example.com"}'

# تعديل مستخدم
curl -X PUT http://localhost:3000/users/1 \
  -H "Content-Type: application/json" \
  -d '{"name":"أحمد محمد الجديد"}'

# حذف مستخدم
curl -X DELETE http://localhost:3000/users/1
```

### باستخدام المتصفح:
- افتح: http://localhost:3000
- للحصول على جميع المستخدمين: http://localhost:3000/users

## 📁 هيكل المشروع

```
backend/
├── db.js                 # الاتصال بقاعدة البيانات PostgreSQL
├── server.js             # ملف تشغيل الخادم الرئيسي
├── routes/
│   └── users.js         # مسارات المستخدمين (CRUD)
├── database.sql         # سكريبت إنشاء الجداول
├── package.json         # معلومات المشروع والحزم
├── config.env.example   # مثال على ملف الإعدادات
└── README.md           # هذا الملف
```

## 🔧 الميزات

- ✅ **Express.js** - إطار عمل سريع ومرن
- ✅ **PostgreSQL** - قاعدة بيانات قوية وموثوقة
- ✅ **CRUD كامل** - جميع العمليات (إنشاء، قراءة، تحديث، حذف)
- ✅ **Async/Await** - كود نظيف وسهل القراءة
- ✅ **معالجة الأخطاء** - Try/Catch لجميع العمليات
- ✅ **التحقق من البيانات** - فحص البيانات قبل الإدخال
- ✅ **CORS** - دعم الطلبات من مصادر مختلفة
- ✅ **رسائل عربية** - جميع الرسائل باللغة العربية

## 🛡️ التحقق من البيانات

المشروع يتضمن:
- التحقق من وجود البيانات المطلوبة
- منع تكرار البريد الإلكتروني
- التحقق من وجود المستخدم قبل التعديل/الحذف
- رسائل خطأ واضحة ومفهومة

## 📝 ملاحظات مهمة

1. تأكد من تشغيل PostgreSQL قبل تشغيل الخادم
2. تأكد من إنشاء قاعدة البيانات `testdb`
3. تأكد من تنفيذ ملف `database.sql` لإنشاء الجداول
4. يمكنك تغيير بيانات الاتصال في ملف `db.js`

## 🐛 حل المشاكل الشائعة

### مشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل:**
- تأكد من تشغيل PostgreSQL
- تحقق من صحة بيانات الاتصال (اسم المستخدم، كلمة المرور، اسم قاعدة البيانات)
- تأكد من أن قاعدة البيانات `testdb` موجودة

### مشكلة: الجدول غير موجود
**الحل:**
```bash
psql -U postgres -d testdb -f database.sql
```

### مشكلة: المنفذ 3000 مستخدم
**الحل:**
- غير المنفذ في ملف `server.js`
- أو أوقف العملية التي تستخدم المنفذ 3000

## 📞 الدعم

إذا واجهت أي مشاكل، تحقق من:
1. سجلات الخادم (Console Logs)
2. سجلات قاعدة البيانات PostgreSQL
3. تأكد من تثبيت جميع الحزم بشكل صحيح

## 📄 الترخيص

هذا المشروع للأغراض التعليمية.

---

**صُنع بـ ❤️ باستخدام Node.js و PostgreSQL**

