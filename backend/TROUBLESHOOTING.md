# 🔧 حل مشكلة ERR_CONNECTION_REFUSED

## ❌ المشكلة:
```
Ce site est inaccessible
localhost n'autorise pas la connexion.
ERR_CONNECTION_REFUSED
```

## ✅ الحل:

هذا الخطأ يعني أن الخادم **غير مشغّل حالياً**. اتبع الخطوات التالية:

---

## 📋 خطوات الحل:

### الخطوة 1: تثبيت المكتبات المطلوبة
افتح Terminal (PowerShell أو CMD) في مجلد `backend` ثم نفذ:

```bash
npm install
```

انتظر حتى ينتهي التثبيت (قد يأخذ 1-2 دقيقة).

---

### الخطوة 2: إعداد قاعدة البيانات PostgreSQL

⚠️ **هذه الخطوة مهمة جداً!**

#### أ. تأكد من تشغيل PostgreSQL:
- ابحث عن "Services" في Windows
- ابحث عن "postgresql"
- تأكد أنه يعمل (Running)

#### ب. أنشئ قاعدة البيانات:
افتح Terminal وشغّل:

```bash
psql -U postgres
```

إذا طلب كلمة مرور، أدخل كلمة مرور PostgreSQL (الافتراضية: `postgres` أو `admin`).

ثم نفذ:

```sql
CREATE DATABASE testdb;
\q
```

#### ج. أنشئ جدول المستخدمين:

```bash
psql -U postgres -d testdb -f database.sql
```

---

### الخطوة 3: تشغيل الخادم

```bash
npm start
```

يجب أن ترى رسالة:
```
🚀 الخادم يعمل على المنفذ 3000
📍 الرابط: http://69.169.108.182:3000
```

---

### الخطوة 4: اختبار الخادم

افتح المتصفح وادخل على:
```
http://69.169.108.182:3000
```

يجب أن ترى رسالة ترحيب.

---

## 🆘 إذا استمرت المشكلة:

### المشكلة 1: PostgreSQL غير مثبت
**الحل:** قم بتثبيت PostgreSQL من:
https://www.postgresql.org/download/windows/

أثناء التثبيت، احفظ كلمة المرور التي تدخلها!

---

### المشكلة 2: خطأ في الاتصال بقاعدة البيانات
**الأعراض:**
```
❌ خطأ في الاتصال بقاعدة البيانات
```

**الحل:**
عدّل ملف `db.js` وغيّر كلمة المرور:

```javascript
const pool = new Pool({
  user: 'postgres',
  host: 'localhost',
  database: 'testdb',
  password: 'كلمة_المرور_الصحيحة', // غيّر هنا
  port: 5432,
});
```

---

### المشكلة 3: المنفذ 3000 مستخدم
**الأعراض:**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**الحل 1 - أوقف العملية القديمة:**
```bash
# في PowerShell
Get-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess | Stop-Process -Force
```

**الحل 2 - غيّر المنفذ:**
افتح `server.js` وغيّر:
```javascript
const PORT = 3001; // بدلاً من 3000
```

---

### المشكلة 4: Node.js غير مثبت
**الأعراض:**
```
'node' is not recognized as an internal or external command
```

**الحل:** قم بتثبيت Node.js من:
https://nodejs.org/

اختر النسخة LTS (الموصى بها).

---

## 📞 اختبار سريع

### اختبار 1: هل Node.js مثبت؟
```bash
node --version
```
يجب أن ترى رقم الإصدار (مثل: v18.17.0)

### اختبار 2: هل npm يعمل؟
```bash
npm --version
```
يجب أن ترى رقم الإصدار (مثل: 9.6.7)

### اختبار 3: هل PostgreSQL يعمل؟
```bash
psql --version
```
يجب أن ترى رقم الإصدار (مثل: psql 16.1)

### اختبار 4: هل قاعدة البيانات موجودة؟
```bash
psql -U postgres -l
```
يجب أن ترى `testdb` في القائمة.

---

## ✅ الخطوات الصحيحة بالترتيب:

```bash
# 1. تثبيت المكتبات
npm install

# 2. إنشاء قاعدة البيانات
psql -U postgres -c "CREATE DATABASE testdb;"

# 3. إنشاء الجداول
psql -U postgres -d testdb -f database.sql

# 4. تشغيل الخادم
npm start

# 5. في نافذة Terminal أخرى (اختبار اختياري)
npm run test:api
```

---

## 🎯 بعد حل المشكلة:

افتح المتصفح على: **http://69.169.108.182:3000/users**

يجب أن ترى قائمة المستخدمين بصيغة JSON.

---

## 📝 ملاحظة مهمة:

**الخادم يجب أن يبقى مشغّلاً في Terminal!**

لا تغلق نافذة Terminal التي تشغل `npm start`، وإلا سيتوقف الخادم وستحصل على نفس الخطأ.

---

لمزيد من المساعدة، راجع:
- `README.md` - التوثيق الكامل
- `تعليمات_التشغيل.md` - التعليمات بالعربية
- `QUICK_START.md` - دليل البدء السريع

