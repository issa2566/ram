# 🚀 سكربت إعداد GitHub التلقائي

سكربت Node.js يقوم بإعداد مشروعك وربطه بـ GitHub تلقائيًا.

## ✨ المميزات

- ✅ التحقق من وجود مجلدات Frontend و Backend
- ✅ إنشاء مستودع جديد على GitHub تلقائيًا
- ✅ إضافة جميع الملفات وعمل Commit تلقائي
- ✅ ربط المشروع بـ GitHub (HTTPS أو SSH)
- ✅ دفع الملفات إلى الفرع الرئيسي تلقائيًا
- ✅ التعامل مع الأخطاء بشكل ذكي
- ✅ طباعة رابط المستودع في النهاية

## 📋 المتطلبات

- Node.js (الإصدار 12 أو أحدث)
- Git مثبت على النظام
- GitHub Personal Access Token

## 🔑 إنشاء GitHub Personal Access Token

1. اذهب إلى: https://github.com/settings/tokens
2. انقر على "Generate new token" → "Generate new token (classic)"
3. اختر الصلاحيات المطلوبة:
   - ✅ `repo` (Full control of private repositories)
4. انسخ الـ Token واحفظه في مكان آمن

## 🚀 طريقة الاستخدام

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. تشغيل السكربت

```bash
node github-setup.js
```

أو:

```bash
npm start
```

### 3. اتباع التعليمات

السكربت سيسألك عن:
- اسم المستودع على GitHub
- GitHub Personal Access Token
- هل تريد المستودع خاص أم عام
- هل تريد استخدام SSH أم HTTPS

## 📝 مثال على الاستخدام

```bash
$ node github-setup.js

🚀 بدء إعداد المستودع على GitHub...

1️⃣  التحقق من تثبيت Git...
✅ Git مثبت

2️⃣  التحقق من بنية المشروع...
✅ تم العثور على: Frontend Backend

3️⃣  جمع المعلومات المطلوبة...
📝 اسم المستودع على GitHub: my-awesome-project
🔑 GitHub Personal Access Token: ghp_xxxxxxxxxxxx
🔒 هل تريد المستودع خاص؟ (y/n): n
🔐 هل تريد استخدام SSH بدلاً من HTTPS？ (y/n): n

...

✅ تم إعداد المستودع بنجاح!

🔗 رابط المستودع:
   https://github.com/username/my-awesome-project
```

## 🔧 الملفات المطلوبة

السكربت يعمل مع أي مشروع يحتوي على:
- مجلد `frontend` أو `Frontend` أو `client` أو `Client`
- مجلد `backend` أو `Backend` أو `server` أو `Server`

إذا لم تكن هذه المجلدات موجودة، السكربت سيسألك إذا كنت تريد المتابعة.

## 📁 بنية المشروع

```
project-root/
├── frontend/          # أو Frontend, client, Client
├── backend/           # أو Backend, server, Server
├── github-setup.js    # السكربت الرئيسي
├── package.json
└── README.md
```

## ⚠️ ملاحظات مهمة

1. **الـ Token**: لا تشارك الـ Token مع أي شخص. السكربت لا يحفظه في أي مكان.

2. **الفرع الرئيسي**: السكربت يستخدم فرع `main` افتراضيًا. إذا كان لديك فرع آخر، يمكنك تغييره يدويًا.

3. **الملفات الكبيرة**: تأكد من وجود `.gitignore` مناسب لتجنب إضافة ملفات كبيرة غير ضرورية.

4. **SSH vs HTTPS**: 
   - **HTTPS**: أسهل في الاستخدام، لكن يحتاج Token في كل مرة
   - **SSH**: يحتاج إعداد مسبق للمفاتيح، لكن أسهل بعد الإعداد

## 🐛 حل المشاكل

### خطأ: "Git غير مثبت"
```bash
# Windows (باستخدام Chocolatey)
choco install git

# macOS
brew install git

# Linux
sudo apt-get install git
```

### خطأ: "فشل إنشاء المستودع"
- تأكد من صحة الـ Token
- تأكد من أن الـ Token لديه صلاحية `repo`
- تأكد من أن اسم المستودع غير موجود بالفعل

### خطأ: "فشل دفع الملفات"
- تأكد من وجود اتصال بالإنترنت
- إذا كان المستودع موجودًا بالفعل، قد تحتاج إلى `git pull` أولاً
- تأكد من صحة معلومات الـ remote

## 📄 الترخيص

MIT License

## 🤝 المساهمة

نرحب بأي مساهمات أو تحسينات!

